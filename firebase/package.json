{
  "name": "harvesthub-firebase",
  "private": true,
  "type": "module",
  "engines": {"node": ">=20"},
  "scripts": {
    "emulators": "firebase emulators:start --config ../firebase.json --project demo-harvesthub",
    "test:rules": "firebase emulators:exec --config ../firebase.json --project demo-harvesthub --only auth,firestore,storage \"node --test --test-concurrency=1 test/rules.test.mjs test/seed.test.mjs\"",
    "seed:emulator": "node seed.mjs --emulator",
    "seed:project": "node seed.mjs"
  },
  "dependencies": {"firebase-admin": "^13.0.0"},
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.3",
    "firebase": "^10.14.1",
    "firebase-tools": "^13.35.1"
  }
}
